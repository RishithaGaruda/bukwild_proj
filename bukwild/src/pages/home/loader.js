import Loader from "react-loader-spinner";

const PageLoader = () => {
    return (
        <Loader
          type="Bars"
          color="#00BFFF"
          height={50}
          width={50}
        />
      );

};

export default PageLoader